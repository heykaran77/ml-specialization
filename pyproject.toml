[project]
name = "ml-specialization"
version = "0.1.0"
description = "Machine Learning Specialization labs and notes"
requires-python = ">=3.11"
dependencies = [
  "jupyter",
  "numpy",
  "matplotlib",
  "tensorflow",
]
